version: "3.8"
services:
  travily-mcp:
    build: .
    environment:
      TRAVLY_KEY: "${TRAVLY_KEY}"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
